{provider_hooks, [
    {pre, [
        {compile, {thrift, compile}},
        {clean, {thrift, clean}}
    ]}
]}.

{thrift_compiler_opts, [
    {in_dir, "damsel/proto"},
    {in_files, ["cds.thrift"]},
    {out_hrl_dir, "src"},
    {gen, "erlang:app_prefix=cds"}
]}.